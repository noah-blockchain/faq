const config = {
    languages: ['en'],
    api: 'https://admin-backend.noah-blockchain.com/api/v1',
    localhost: 'http://localhost:1337/api/v1',
    headers:{
        json: {
            headers: {
                'Content-Type': 'application/json'
            }
    }
  }
}

export default config